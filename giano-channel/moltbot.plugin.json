{
  "id": "giano-channel",
  "channels": ["giano-channel"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "targetBotUserId": {
        "type": "string",
        "description": "User ID of the mcp-giano-bridge bot to send tasks to"
      }
    }
  }
}
